# Normalize line endings to LF in the repository (and on checkout).
* text=auto eol=lf

# Treat lockfiles as generated: keep them in the repo but collapse their diffs.
package-lock.json -diff
pnpm-lock.yaml -diff
